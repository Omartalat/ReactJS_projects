{
  "compilerOptions": {
    /* 1. Environment & Output */
    "target": "ES2022",                               /* Generates modern JavaScript for Node.js */
    "module": "NodeNext",                             /* Critical for "type": "module" in package.json */
    "moduleResolution": "NodeNext",                   /* Critical for correct import resolutions */
    "rootDir": "./src",                               /* Where your TS files are */
    "outDir": "./dist",                               /* Where the compiled JS goes */
    
    /* 2. Strict Type-Checking (The "Job-Ready" Standard) */
    "strict": true,                                   /* Enables all strict type checking options */
    "noUncheckedIndexedAccess": true,                 /* Prevents crashes when accessing array[index] that doesn't exist */
    "forceConsistentCasingInFileNames": true,         /* Prevents file name bugs across OS (Windows/Mac) */
    
    /* 3. Interop & Build Speed */
    "esModuleInterop": true,                          /* Allows importing CommonJS packages (like older libs) */
    "skipLibCheck": true,                             /* Skips type checking of .d.ts files in node_modules (faster build) */
    "types": ["node"]                                 /* Ensures we only load Node.js types, not DOM/Frontend types */
  },
  "include": ["src/**/*"],                            /* Only compile files in the src folder */
  "exclude": ["node_modules"]                         /* Ignore external libraries */
}